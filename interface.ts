// Interface is a structure that defines the contract of your application or syntax for flow of class
interface studentDetials {
  name: string;
  age: number;
  subject: string;

  getName: (name) => string;
  getAge(number): number;
}
